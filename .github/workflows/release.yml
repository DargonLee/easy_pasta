name: Build and Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., v1.0.0)'
        required: true
        default: 'v1.0.0'
      release_notes:
        description: 'Release notes'
        required: false
        default: 'New release'

jobs:
  build-macos:
    name: Build and Release macOS
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.4'
          channel: 'stable'
      
      - name: Flutter doctor
        run: flutter doctor -v
      
      - name: Get dependencies
        run: flutter pub get
      
      - name: Build macOS app
        run: flutter build macos --release
      
      - name: Create ZIP archive
        run: |
          cd build/macos/Build/Products/Release
          zip -r EasyPasta-${{ github.event.inputs.version }}.zip EasyPasta.app
          mv EasyPasta-${{ github.event.inputs.version }}.zip $GITHUB_WORKSPACE/
      
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.event.inputs.version }}
          release_name: EasyPasta ${{ github.event.inputs.version }}
          body: |
            ## EasyPasta ${{ github.event.inputs.version }}
            
            ${{ github.event.inputs.release_notes }}
            
            ### 📦 下载
            
            - **macOS**: EasyPasta-${{ github.event.inputs.version }}.zip
            
            ### 📝 安装说明
            
            1. 下载 `EasyPasta-${{ github.event.inputs.version }}.zip`
            2. 解压并将 `EasyPasta.app` 拖入 `Applications` 文件夹
            3. 首次打开可能需要在"系统偏好设置 > 安全性与隐私"中允许
            4. 使用 `Cmd+Shift+V` 唤起剪贴板面板
            
            ### ✨ 主要功能
            
            - 📋 自动保存剪贴板历史
            - 🔍 智能搜索与筛选
            - 🎨 支持文本、图片、文件等多种格式
            - ⭐ 收藏常用内容
            - 🌓 完美的深浅模式支持
            
            ---
            
            **最低系统要求**: macOS 10.15+
          draft: false
          prerelease: false
      
      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: EasyPasta-${{ github.event.inputs.version }}.zip
          asset_name: EasyPasta-${{ github.event.inputs.version }}.zip
          asset_content_type: application/zip
